apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: masterlion-postgres
spec:
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: masterlion-test-essd-retain
        resources:
          requests:
            storage: 20Gi
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: masterlion-redis
spec:
  volumeClaimTemplates:
    - metadata:
        name: redis-data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: masterlion-test-essd-retain
        resources:
          requests:
            storage: 20Gi
