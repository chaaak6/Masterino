apiVersion: v1
kind: Service
metadata:
  name: masterlion
  labels:
    app.kubernetes.io/name: masterlion
    app.kubernetes.io/part-of: masterlion
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: masterlion
  ports:
    - name: http
      port: 3210
      targetPort: 3210
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: masterlion
  labels:
    app.kubernetes.io/name: masterlion
    app.kubernetes.io/part-of: masterlion
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: masterlion
  template:
    metadata:
      labels:
        app.kubernetes.io/name: masterlion
        app.kubernetes.io/part-of: masterlion
    spec:
      containers:
        - name: masterlion
          image: boen-registry-vpc.cn-shenzhen.cr.aliyuncs.com/biel_client/masterlion:v1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3210
              name: http
          envFrom:
            - secretRef:
                name: masterlion-secret
            - configMapRef:
                name: masterlion-config
          env:
            # The application must never connect directly to the Aihub database.
            - name: AIHUB_READONLY_DATABASE_URL
              value: ""
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "1"
              memory: 2Gi
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 3
          startupProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
            failureThreshold: 30
