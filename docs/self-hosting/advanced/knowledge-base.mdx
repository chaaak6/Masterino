---
title: MasterLion Knowledge Base / File Upload
description: >-
  Explore MasterLion's file upload and knowledge base management features with core
  components.
tags:
  - MasterLion
  - File Upload
  - Knowledge Base
  - PostgreSQL
  - OpenAI Embedding
---

# Knowledge Base / File Upload

MasterLion supports file upload and knowledge base management. This feature relies on the following core technical components. Understanding these components will help you successfully deploy and maintain the knowledge base system.

## Core Components

### 1. PostgreSQL and PGVector

PostgreSQL is a powerful open-source relational database system, and PGVector is its extension for vector operations.

- **Purpose**: Store structured data and vector indexes
- **Deployment Tip**: Use the ParadeDB Docker image for quick deployment with pgvector and pg\_search plugins

Deployment script example:

```
docker run -p 5432:5432 -d --name pg -e POSTGRES_PASSWORD=mysecretpassword paradedb/paradedb:latest-pg17
```

- **Note**: Ensure sufficient resources for vector operations

### 2. S3-compatible Object Storage

S3 (or S3-compatible storage services) is used for storing uploaded files.

- **Purpose**: Store raw files
- **Options**: AWS S3, RustFS, ceph, or other S3-compatible services
- **Note**: Configure appropriate access permissions and security policies

For storage A/B diagnostics, you can add `docker-compose.rustfs.yml` to the deploy compose command
to point new uploads at RustFS with `S3_ENABLE_PATH_STYLE=1`. This does not migrate historical
objects from COS/OSS; full migration requires copying existing object keys to RustFS or adding a
dual-read fallback.

### 3. Embedding Model

An embedding model converts parsed text into vector representations for semantic search.

<Callout type={'info'}>
  MasterLion's historical fallback is OpenAI `text-embedding-3-small`. In an Aihub-backed
  deployment, set `DEFAULT_FILES_CONFIG` to an embedding model that Aihub exposes to the managed
  token or user group. Otherwise chunking can succeed while embedding tasks fail with
  `ModelNotFound`.
</Callout>

- **Purpose**: Generate vector representations for semantic search
- **Notes**:
  - Requires an embedding-capable model in the configured provider
  - For Aihub integration, use the `newapi` provider prefix and the exact model id granted by Aihub
  - Object storage such as RustFS, MinIO, OSS, or COS stores files only; it does not parse, chunk, or vectorize content

### 4. Unstructured.io (Optional)

Unstructured.io is a powerful document processing tool.

- **Purpose**: Process complex document formats, extract structured information
- **Use Case**: Handle non-plain text formats like PDF, Word
- **Note**: Evaluate processing needs based on document complexity

By correctly configuring and integrating these core components, you can build a powerful and efficient knowledge base system for MasterLion. Each component plays a crucial role in the overall architecture, supporting advanced document management and intelligent retrieval functions.

### 5. Custom Embedding

- **Purpose**: Use different Embedding generate vector representations for semantic search
- **Options**: support model provider list: newapi/zhipu/github/openai/bedrock/ollama
- **Deployment Tip**: Used to configure the default Embedding model

```
environment:
  DEFAULT_FILES_CONFIG: embedding_model=newapi/YOUR_AIHUB_EMBEDDING_MODEL,query_mode=full_text
  CHUNKS_AUTO_EMBEDDING: '1'
```

If you only need uploads and full-text parsing, keep `CHUNKS_AUTO_EMBEDDING=0`; existing files with
failed embedding tasks need to be re-uploaded or retried after the model config is fixed.
